{
  "name": "@ryu/sdk-native",
  "version": "0.1.0",
  "description": "Native (napi-rs) Node addon backed by the ryu-sdk Rust core.",
  "main": "index.js",
  "types": "index.d.ts",
  "files": ["index.js", "index.d.ts", "*.node"],
  "napi": {
    "name": "ryu_sdk_napi"
  },
  "scripts": {
    "build": "napi build --platform --release",
    "build:debug": "napi build --platform",
    "prepare": "napi build --platform"
  },
  "devDependencies": {
    "@napi-rs/cli": "^2.18.4"
  }
}
