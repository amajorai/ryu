{
  "name": "core",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "dev": "node scripts/dev.js",
    "dev:watch": "cargo watch -x run",
    "build": "cargo build --release",
    "check": "cargo check"
  }
}
