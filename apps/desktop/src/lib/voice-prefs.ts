// Shared localStorage keys for desktop voice-mode display preferences, so the
// settings toggle and the runtime surface read the exact same key.

/** Show the transcript panel (ON) vs. the classic full-screen orb (OFF). */
export const VOICE_SHOW_TRANSCRIPT_KEY = "ryu:voice-show-transcript";
