// Re-export shim: the tiny markdown renderer now lives in @ryu/blocks/island so
// the storyboard renders the real assistant-reply formatting.

export { Markdown } from "@ryu/blocks/island/chat/markdown";
