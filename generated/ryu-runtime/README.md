# Generated Ryu runtime package assets

This directory is emitted by the private monorepo mirror so the public Core build can embed built-in manifests and referenced assets. It is not an app or plugin authoring surface. App source lives in `amajorai/ryu-<app>` satellites; plugin source lives in `amajorai/ryu-marketplace`.
